package defpackage;

import java.io.Closeable;
import java.io.File;
import java.io.OutputStream;
import java.util.Map;

/* renamed from: odt */
public interface odt extends Closeable {
    odt a();

    odt a(abye abye);

    odt a(abyl abyl);

    odt a(abyo abyo);

    odt a(abyy abyy);

    odt a(aeew aeew);

    odt a(Map<Integer, ? extends kaz<jwj>> map);

    odt a(kaz<jwj> kaz, ohy ohy);

    odt a(ohz ohz);

    odt a(boolean z);

    File b();

    OutputStream c();

    odt d();

    abyi e();
}
